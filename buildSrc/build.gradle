plugins {
    id 'groovy-gradle-plugin'
}
gradlePlugin {
    plugins {
        javaNatives {
            id = 'java-native'
            implementationClass = 'com.niton.gradle.plugin.natives.NativePlugin'
        }
    }
}